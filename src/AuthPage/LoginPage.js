import { SafeAreaView, Text } from 'react-native';

function LoginPage() {
    return (
       <SafeAreaView>
            <Text>
                loginPage
            </Text>
       </SafeAreaView>
    );
}

export default LoginPage;